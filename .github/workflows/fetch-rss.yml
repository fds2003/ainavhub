name: 获取AI新闻数据

on:
  schedule:
    - cron: '0 */4 * * *'  # 每4小时运行一次（北京时间凌晨2点、6点、10点等）
  workflow_dispatch:        # 允许手动触发
  push:
    branches: [ main ]      # main分支推送时触发

jobs:
  fetch-rss:
    runs-on: ubuntu-latest

    steps:
    - name: 检出代码
      uses: actions/checkout@v3

    - name: 设置Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '18'

    - name: 安装依赖
      run: |
        cd scripts
        npm init -y
        npm install rss-parser axios

    - name: 获取RSS数据
      run: node scripts/fetch-rss.js

    - name: 提交更新
      run: |
        git config --local user.email "action@github.com"
        git config --local user.name "AiNavHub Bot"
        git add data/rss-data.json
        git diff --quiet && git diff --staged --quiet || git commit -m "🔄 更新AI新闻数据 - $(date +'%Y-%m-%d %H:%M')"

    - name: 推送到仓库
      uses: ad-m/github-push-action@master
      with:
        github_token: ${{ secrets.GITHUB_TOKEN }}
        branch: ${{ github.ref }}